NODE_ENV=development
PORT=4000
LOG_LEVEL=info
# ThingsBoard Configuration
# Base URL of ThingsBoard instance
THINGSBOARD_BASE_URL=https://portal.indusmind.net

# ThingsBoard credentials
# Use a dedicated service account for API access, not a user account

THINGSBOARD_USERNAME=indusmind.admin@indusmind.io
THINGSBOARD_PASSWORD=IndrT--968$$$1dfvcJUIop

# Optional: Token refresh strategy
# REFRESH_BEFORE_EXPIRY_MS: How many milliseconds before token expiry to refresh (default: 120000 = 2 minutes)
THINGSBOARD_TOKEN_REFRESH_BEFORE_EXPIRY_MS=120000

# Optional: API timeout settings
THINGSBOARD_API_TIMEOUT_MS=30000
THINGSBOARD_AUTH_TIMEOUT_MS=10000
